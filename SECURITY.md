# Security policy

- Never commit `.env`.
- Never paste Deriv tokens into chat, screenshots, issues, pull requests, logs, or frontend code.
- Use the minimum token scopes required.
- Use paper mode first and a virtual/demo account for execution testing.
- This project refuses demo execution when the authorisation response is not marked virtual.
- Revoke a token immediately if it is exposed.
